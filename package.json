{
  "name": "ha-gtfs-mta-card",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "tsc src/* --outDir dist",
    "prebuild": "yarn clean"
  },
  "devDependencies": {
    "@types/node": "^20.11.20",
    "typescript": "^5.3.3"
  }
}
